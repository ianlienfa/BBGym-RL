#ifndef SEARCH_GRAPH_H
#define SEARCH_GRAPH_H

/*----------------------------------------------------------------------------
 * Struct: SearchGraph
 * Specification: The struct for the search graph, keeps track of the search status
 * Usage: 
    * For different Branch and Bound algorithms, the search graph is different,
        users need to implement the search graph for different algorithms.
    * The search graph is accessible by searcher, brancher and pruner.
    * The search graph is also accessible by the BB_engine.
 * Constructor: ...
----------------------------------------------------------------------------
 
----------------------------------------------------------------------------*/

struct SearchGraph
{

    // other search strategies related data structures...
};

#endif